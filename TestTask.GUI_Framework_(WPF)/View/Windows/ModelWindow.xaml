<UserControl x:Class="TestTask.GUI_Framework__WPF_.View.Windows.ModelWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestTask.GUI_Framework__WPF_.View.Windows"
             xmlns:vm="clr-namespace:TestTask.GUI_Framework__WPF_.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300">
    <UserControl.DataContext>
        <vm:ModelWindowViewModel/>
    </UserControl.DataContext>
    <Border Background="White" Padding="10" BorderBrush="Gray" BorderThickness="1">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

       <!--Заголовок модального окна -->

        <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="Bold" Grid.Row="0" Margin="0 0 0 10"/>
        
        <!--Определение динамического контента-->

        <ContentControl Content="{Binding DialogContent}" Grid.Row="1" Margin="0 10 0 10"/>
        
        <!--Кнопки-->

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Подтвердить" Width="80" Margin="0 0 0 10" Command="{Binding OkCommand}"/>
            <Button Content="Отменить" Width="80" Margin="0 0 0 10" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
    </Border>
</UserControl>
